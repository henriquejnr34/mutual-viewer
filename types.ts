
export interface User {
  id: string;
  name: string;
  username: string;
  profileImageUrl: string;
}

export interface AuthenticatedUser {
  id: string;
  name: string;
  username: string;
  profileImageUrl: string;
}
